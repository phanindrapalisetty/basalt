# Version Document v0.1.0

## Folder Structure
basalt/
├── api/
│   └── handler.py
├── core/
│   ├── spec.py
│   ├── constraints.py
│   ├── generators.py
│   └── random.py
├── tests/
│   └── test_determinism.py
├── README.md
└── requirements.txt

## High Level Design
HTTP POST /generate
        |
        v
+----------------------+
| Spec Validator       |
+----------------------+
        |
        v
+----------------------+
| RandomContext (seed) |
+----------------------+
        |
        v
+----------------------+
| Dataset Generator    |
+----------------------+
        |
        v
+----------------------+
| JSON Serializer      |
+----------------------+
        |
        v
HTTP 200 (JSON array)

### Python venv creation: 
```
python -m venv basalt-venv 
source basalt-venv/bin/activate
```

### FASTAPI uvicorn command:
`uvicorn api.handler:app --reload --host 0.0.0.0 --port 8000`

### Handle File dependencies